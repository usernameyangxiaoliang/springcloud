server:
  port: 8888
spring:
  application:
    name: cloud-consumer-order-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
